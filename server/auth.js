var db = require('./db.js').db,
    q = require('q');

module.exports = {
  authFacebook : function() {
  },

  authTwitter : function() {
  },

  authEmailPassword : function(userEmail, userPass) {
  },

  resetPassword : function(userEmail, oldPass, newPass) {
  },

  logout : function() {
  },

  isValid : function(userEmail) {
  }
};

